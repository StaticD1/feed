services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: feed
      POSTGRES_USER: feed
      POSTGRES_PASSWORD: feed
    ports:
      - "5432:5432"
    volumes:
      - feed_postgres_data:/var/lib/postgresql/data

volumes:
  feed_postgres_data:
